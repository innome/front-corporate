import { AppWrapper } from '@components/AppWrapper';
import { ProductosPage } from '@components/pages/ProductosPage';

export const ProductosEntry = () => (
	<AppWrapper defaultTheme="white">
		<ProductosPage />
	</AppWrapper>
);
