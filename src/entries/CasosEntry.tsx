import { AppWrapper } from '@components/AppWrapper';
import { CasosPage } from '@components/pages/CasosPage';

export const CasosEntry = () => (
	<AppWrapper defaultTheme="white">
		<CasosPage />
	</AppWrapper>
);
